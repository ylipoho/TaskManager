<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="pleasework.View.TasklistPage">
    
    <ContentPage.Content>
        <ScrollView>
            <ListView 
                x:Name="TaskListView"
                HasUnevenRows="True"
                Margin="10"
                ItemsSource="{Binding Tasks}"
                ItemTapped="TaskListView_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Vertical">
                                <Label 
                                    Text="{Binding Title}"
                                    FontSize="Medium">
                                </Label>

                                <Label 
                                    Text="{Binding Deadline}" >
                                </Label>

                                <Label 
                                    Text="{Binding IsDone, StringFormat='done:'}" >
                                </Label>

                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>